#!/usr/bin/env bash
# of-run: run an OpenFOAM command; needs interactive bash so shell functions (foamVersion) exist
# Usage: of-run <command> [args...]
set -euo pipefail

if [ $# -lt 1 ]; then
  echo "Usage: of-run <command> [args...]" >&2
  exit 2
fi

ENGINE="${OPENFOAM_ENGINE:-docker}"
IMAGE="${OPENFOAM_IMAGE:-docker.io/opencfd/openfoam-default}"
TAG="${OPENFOAM_TAG:-2406}"

UIDGID_ARGS=()
if [ "${ENGINE}" = "docker" ] || [ "${ENGINE}" = "podman" ]; then
  UIDGID_ARGS=(--user "$(id -u)":"$(id -g)")
fi

CMD="$*"

# Belangrijk verschil: voeg -t toe, geen -i
exec "${ENGINE}" run --rm -t \
  "${UIDGID_ARGS[@]}" \
  -v "$PWD:$PWD" -w "$PWD" \
  "${IMAGE}:${TAG}" \
  -- bash -ic "$CMD"

