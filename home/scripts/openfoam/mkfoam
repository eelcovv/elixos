#!/usr/bin/env bash
# mkfoam: Create a minimal ParaView .foam file in the current directory.
#
# Key points:
# - Generates <name>.foam file (default: case.foam).
# - ParaView detects .foam files and auto-loads OpenFOAM cases.
#
# Usage:
#   mkfoam [name]
# Example:
#   mkfoam mycase   → creates mycase.foam

set -euo pipefail

NAME="${1:-case}"
touch "${NAME}.foam"
echo "Created: ${NAME}.foam (open in ParaView)."

